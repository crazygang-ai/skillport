import SwiftUI

struct RootView: View {
    var body: some View {
        VStack {
            Text("Skillport")
                .font(.largeTitle)
            Text("Loading…")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
